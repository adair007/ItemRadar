# Changelog

所有值得记录的变更都在此文档中列出。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [未发布]
### 待定

- （占位，后续新功能可写在这里）

## [1.1.0] - 2026-08-17

### Added

- 版本更新检查：每日首次打开面板时查询最新版本，有更新时在状态图标显示红点，并支持手动「检查更新」。

### Changed

- 项目编辑重构：将「编辑名称 / 命令 / 网址」三个入口合并为统一的「编辑…」独立窗口（填写所有字段后一次性保存），并移除表单中的帮助文案。
- 头部提示改为即时显示（quickHelp，无系统延迟）。
- 「从指定文件夹获取」更名为「从指定文件夹获取项目」；「检查更新」图标改为 `arrow.up.circle`。

### Fixed

- 修复弹窗内文本框无法输入的问题：popover 行为由 `.transient` 改为 `.semitransient`，并配合应用激活与 `makeKey()` 保证键盘焦点。
- 修复点击弹窗外部无法关闭、以及未保存编辑被意外保留的问题：改用全局鼠标监听关闭弹窗，并在关闭时重建视图以丢弃未保存编辑。
- 修复 tooltip 文字不可见的问题（黑字黑底）：改用 `foregroundStyle(.white)` 与圆角背景。
- 修复更新检查因 GitHub REST API 限流（403）失败的问题：改用 `git ls-remote --tags` 查询版本 tag，无需 token。

### Performance

- 按需刷新：点击菜单栏图标不再自动重扫，仅在手动刷新 / 添加文件夹 / 配置变更时刷新。
- URL 探测改为 `DispatchSourceTimer` 定时驱动 + 日志增量解析，移除忙等线程；端口等待改为 1 秒间隔定时器。
- 批量 `lsof`：单次拉取所有监听端口（替代逐端口查询），并带 5 秒防抖。
- 存活检查简化为单次主队列延迟。

## [1.0.0] - 2026-08-16

### Added

- 初始版本：ItemRadar macOS 菜单栏应用，自动发现并管理本机服务。
- 自动扫描目录并推断启动命令（`package.json` / `docker-compose` / Python / Cargo / Go / PHP / .NET / Rails 等）。
- 项目列表展示（名称 / 路径 / 启动命令），一键启动 / 停止。
- 启动后自动探测网页地址并用默认浏览器打开（日志解析 + `lsof` 端口探测）。
- 右键菜单：在 Finder 中显示、打开日志、在浏览器打开、编辑启动命令 / 网页地址、复制启动命令、自动打开浏览器开关、从列表移除。
- 拖拽排序（顺序持久化到配置）。
- 设置窗口：扫描文件夹增删、额外扫描用户目录顶层、开机自启动。
- 手动添加项目（校验路径 / 命令 / 网页地址）。
- 命令行工具：`--scan` / `--start` / `--stop` / `--status` / `--test-url`。
- 配置文件 `~/.projectbar/config.json`（扫描范围与手动项目清单）。
- 应用图标（雷达）与 Dock / Launchpad 入口。
- 项目名称编辑（右键菜单内联编辑）。

### Fixed

- 停止服务时结束整个进程树，避免 turbo 等子进程（运行在独立进程组）残留。
- 开机自启动的 LaunchAgent 改为直接运行应用二进制。
